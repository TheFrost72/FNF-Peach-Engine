<?xml version="1.0" encoding="utf-8"?>
<project>
    <!-- Android-only Neuro Engine -->

    <app title="Friday Night Funkin': Neuro Engine"
         file="Neuro Engine"
         packageName="com.thefrost72.neuroengine"
         package="com.thefrost72.neuroengine"
         main="Main"
         version="1.0.4"
         versionCode="1"
         company="TheFrost72" />

    <set name="APP_ID" value="0x0100f6c013bbc000" />

    <!-- Haxe / engine options -->
    <haxedef name="LINC_LUA_RELATIVE_DYNAMIC_LIB"/>
    <app preloader="flixel.system.FlxPreloader" />
    <set name="SWF_VERSION" value="11.8" />

    <!-- Android-specific defines -->
    <section if="android">
        <define name="TITLE_SCREEN_EASTER_EGG"/>
        <define name="BASE_GAME_FILES" />
        <define name="VIDEOS_ALLOWED"/>
        <define name="MODS_ALLOWED"/>
        <define name="HSCRIPT_ALLOWED"/>
        <define name="LUA_ALLOWED"/>
        <define name="ACHIEVEMENTS_ALLOWED"/>
        <define name="TRANSLATIONS_ALLOWED"/>
        <define name="IRIS_DEBUG"/>
        <define name="MULTITHREADED_LOADING"/>
        <define name="SHOW_LOADING_SCREEN"/>
        <define name="PSYCH_WATERMARKS"/>
    </section>

    <!-- Window settings for mobile -->
    <window if="mobile" orientation="landscape" fullscreen="true" width="0" height="0" resizable="false" allow-high-dpi="true" fps="60" />

    <!-- Build directories -->
    <set name="BUILD_DIR" value="export/release" unless="debug" />
    <set name="BUILD_DIR" value="export/debug" if="debug" />

    <classpath name="source" />

    <!-- Crash handler for Android -->
    <define name="CRASH_HANDLER" if="android" />

    <!-- Assets -->
    <assets path="assets/fonts" />
    <assets path="assets/shared"/>
    <assets path="assets/embed" embed="true"/>
    <assets path="assets/videos" if="VIDEOS_ALLOWED"/>
    <assets path="assets/songs"/>
    <assets path="assets/week_assets"/>
    <assets path="assets/translations"/>
    <assets path="assets/base_game"/>
    <section if="TITLE_SCREEN_EASTER_EGG">
        <assets path="assets/secrets" rename="assets/secrets"/>
    </section>

    <!-- Mods -->
    <section if="MODS_ALLOWED">
        <assets path="example_mods" rename="mods" embed="false" type="template"/>
        <assets path="list.txt" rename="modsList.txt"/>
    </section>

    <assets path="art/readme.txt" rename="do NOT readme.txt"/>

    <!-- Libraries needed for Android -->
    <haxelib name="flixel" version="5.6.1"/>
    <haxelib name="flixel-addons" version="3.2.2"/>
    <haxelib name="tjson" version="1.4.0"/>
    <haxelib name="linc_luajit" if="LUA_ALLOWED"/>
    <haxelib name="hscript-iris" if="HSCRIPT_ALLOWED" version="1.1.3"/>
    <haxelib name="hxvlc" if="VIDEOS_ALLOWED"/>
    <haxelib name="flxanimate"/>

    <!-- Disable unnecessary things -->
    <haxedef name="HXCPP_CHECK_POINTER" if="CRASH_HANDLER"/>
    <haxedef name="HXCPP_STACK_LINE" if="CRASH_HANDLER"/>
    <haxedef name="HXCPP_STACK_TRACE" if="CRASH_HANDLER"/>
    <haxedef name="FLX_NO_FOCUS_LOST_SCREEN"/>
    <haxedef name="FLX_NO_DEBUG" unless="debug"/>
    <haxedef name="NAPE_RELEASE_BUILD" unless="debug"/>
    <haxedef name="no-deprecation-warnings"/>
    <haxedef name="message.reporting" value="pretty"/>

    <!-- Android certificate -->
    <certificate path="neuroengine.keystore" password="NeuroPass72!" alias="neuroengine" alias-password="NeuroPass72!" if="android" unless="debug"/>

    <!-- Touch controls -->
    <define name="TOUCH_CONTROLS" if="mobile"/>

    <!-- Icons -->
    <icon path="art/icons/icon16.png" size="16"/>
    <icon path="art/icons/icon32.png" size="32"/>
    <icon path="art/icons/icon64.png" size="64"/>
    <icon path="art/icons/iconOG.png"/>
</project>
